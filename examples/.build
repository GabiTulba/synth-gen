# Root manifest: the examples tree builds as one project. Libraries are
# discovered dynamically from here (basic/ declares `library Basic`);
# each rule keeps its own build/ output directory. `synthc build` or
# `synthc watch` from this directory builds/watches every rule.
project examples
build basic
build pluck
build primitives
build advanced
build song
build darksynth
