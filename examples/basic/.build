# Basic instrument samples, packaged as a library: every module is
# public (exposed), and each still renders its own demo artifact when
# the library itself is built.
library Basic
expose bassdrum.synth
expose bassdrum_harsh.synth
expose bitonal.synth
expose snare.synth
expose kick.synth
expose guitar.synth
expose piano.synth
