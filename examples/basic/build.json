{
  "library": "Basic",
  "description": "Basic instrument samples, packaged as a library: lib.synth exposes every module, and each still renders its own demo artifact when the library itself is built. Presets over the shared Voices cores, so building this directory needs the examples root in place.",
  "dependencies": ["Voices"]
}
